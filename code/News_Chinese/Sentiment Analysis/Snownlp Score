import pandas as pd
import numpy as np
from snownlp import SnowNLP

# inputs is series of words data
def calculate_sentiment(inputs):
    output = SnowNLP(inputs)
    return output.sentiments

# Sample
df_cu['Sentiment Score'] = df_cu['title'].apply(calculate_sentiment)
